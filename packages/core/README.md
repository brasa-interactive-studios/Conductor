# Core Package (Transition Placeholder)

The existing root `src/` currently contains the AI core runtime.

This folder is reserved for future extraction into a dedicated workspace package without breaking current APIs.
